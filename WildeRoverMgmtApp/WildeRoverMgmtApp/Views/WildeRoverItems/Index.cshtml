@model IEnumerable<WildeRoverMgmtApp.Models.WildeRoverItem>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Par)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Have)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Type)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Details" asp-route-id="@item.WildeRoverItemId">
                    @Html.DisplayFor(modelItem => item.Name)
                </a>                
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Par)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Have)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WildeRoverItemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WildeRoverItemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WildeRoverItemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
